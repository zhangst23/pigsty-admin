# Managed by Pigsty. Readable only by root.
[client]
user={{ mysql_backup_username }}
password="{{ mysql_cluster_password | string | replace('\\', '\\\\') | replace('"', '\\"') }}"
protocol=socket
socket={{ mysql_sock }}
ssl-mode=VERIFY_CA
ssl-ca={{ mysql_tls_ca }}
