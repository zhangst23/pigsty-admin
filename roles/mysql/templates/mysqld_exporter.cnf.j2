# Managed by Pigsty. Readable only by root and prometheus.
[client]
user={{ mysql_monitor_username }}
password="{{ mysql_monitor_password | string | replace('\\', '\\\\') | replace('"', '\\"') }}"
host=127.0.0.1
port=3306
ssl-ca={{ mysql_tls_ca }}
